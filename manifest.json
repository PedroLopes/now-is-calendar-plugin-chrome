{
  "manifest_version": 3,
  "name": "NowIs.Org > Calendar",
  "version": "1.0",
  "description": "Finds events at the amazing now-is.org and adds a button so that you can add them to your calendar (currently only supports google calendar API)",
  "permissions": ["scripting", "activeTab"],
  "content_scripts": [
    {
      "matches": ["https://now-is.org/"],
      "js": ["content.js"]
    }
  ]
}

