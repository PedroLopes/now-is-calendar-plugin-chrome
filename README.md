# NowIs.Org > Calendar

Finds events at the amazing now-is.org and adds a button so that you can add them to your calendar (currently only supports google calendar API). 

TODO: add options page so you can tweak settings. Currently, settings are tweaked via code. 
